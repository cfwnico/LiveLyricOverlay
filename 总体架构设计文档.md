# LiveLyricOverlay 总体架构设计文档

## 一、 概述

LiveLyricOverlay 是一款专为直播场景（特别是 Bilibili 直播姬）设计的 Windows 桌面歌词 Overlay 软件。该软件具备极低的系统资源占用（CPU < 1%）、真正的 Alpha 透明通道支持，以及高精度的同步渲染能力。

本文档基于 `RDP.md` 需求文档，对 LiveLyricOverlay 的模块划分、线程模型、核心类设计及数据流进行详细设计。

---

## 二、 模块划分

系统整体采用**松耦合、高内聚**的三层架构（数据层、引擎层、渲染层），具体划分为以下五大核心模块：

### 1. 数据源与 IPC 模块 (Data Source & IPC)
* **Foobar2000 插件端 (Component)**: 负责在 Foobar2000 内部监听播放事件（Play/Pause/Stop、Track Changed、Seek），并将当前文件路径、实时播放进度通过 IPC（如命名管道或共享内存）广播。
* **数据接收端 (Client)**: 在主程序中接收插件发来的数据，保证毫秒级的状态同步。

### 2. 歌词引擎模块 (Lyric Engine)
* **文件检索 (Lyric Locator)**: 根据当前播放歌曲的绝对路径，按策略查找同名 `.lrc` 文件。
* **文件监视 (File Watcher)**: 监控当前加载的 LRC 文件，一旦外部被修改（如用户通过 ESLyric 重新下载并保存），立即触发热重载。
* **解析器 (Lyric Parser)**: 处理多种文本编码（自动识别 UTF-8/GBK/ANSI 等），解析单行多时间轴标签、偏移量 `[offset:...]`，以及翻译行的分离。
* **同步器 (Synchronizer)**: 纯逻辑模块。接受当前播放时间（ms），返回当前应该高亮显示的歌词行。

### 3. 渲染模块 (Rendering Module)
* **图形 API 层**: 基于 Direct2D (D2D) 和 DirectWrite (DWrite) 实现高性能硬件加速渲染，确保资源消耗极低且支持抗锯齿。
* **特效层 (Effects)**: 实现基于 D2D 的文本描边、发光（高斯模糊）以及阴影效果，支持各种透明度和渐变运算。
* **窗口管理 (Window Output)**: 负责向 Windows 提交 `UpdateLayeredWindow`，实现背景真正的 Alpha=0 透明以及极高的直播捕获兼容性。

### 4. 窗口与交互模块 (Window & UI)
* 管理 Layered Window 的生命周期。
* 处理高 DPI (Per-Monitor DPI-Aware) 缩放逻辑。
* 处理鼠标交互（拖动、右键菜单、穿透与锁定）。

### 5. 配置模块 (Config Manager)
* 基于 JSON 的多套配置文件管理（如“直播”、“录播”、“音乐”等预设配置），供各模块读取界面样式与刷新率。

---

## 三、 线程模型

为满足 `< 1%` CPU 占用和高达 `120Hz` 的刷新率要求，采用多线程模型，将渲染和数据采集与 UI 解耦。

1. **主 UI 线程 (Main UI Thread)**
   * 创建并维护隐藏的主消息窗口、托盘图标和配置界面。
   * 处理全局快捷键、窗口位置拖拽等常规 Windows 消息。
   
2. **渲染与同步线程 (Render & Sync Thread)**
   * 核心工作线程。依赖多媒体定时器 (Multimedia Timer) 或 `WaitableTimer` 驱动。
   * 运行频率由配置的刷新率（15Hz - 120Hz）决定。
   * **工作流程**: 醒来 -> 查询最新播放时间 -> 调用同步器计算当前歌词 -> 调用 D2D 渲染下一帧画面 -> 提交 `UpdateLayeredWindow`。

3. **IPC 数据接收线程 (IPC Receiver Thread)**
   * 专门负责与 Foobar2000 组件通信（如阻塞式监听 Named Pipe）。
   * 收到 `Track Change`、`Seek` 等事件时，原子更新全局状态变量，并通过信号量或事件通知渲染线程立即重绘（消除延迟）。

4. **文件监控线程 (File Watcher Thread - 可选/后台池)**
   * 调用 `ReadDirectoryChangesW`，阻塞等待当前 `.lrc` 文件所在目录的变更，触发重新解析。

---

## 四、 核心类设计

以下为 C++ (或 C#) 的核心面向对象设计（以伪代码和核心接口为主）：

### 1. 数据层
```cpp
// 描述当前播放器状态的数据结构
struct PlaybackState {
    bool isPlaying;
    bool isPaused;
    int64_t currentTimeMs;
    std::wstring currentFilePath;
};

// 数据源接口
class IDataSource {
public:
    virtual ~IDataSource() = default;
    virtual PlaybackState GetCurrentState() = 0;
    // 注册回调：在切歌或Seek时通知主程序
    virtual void SetEventCallback(std::function<void(EventType, PlaybackState)> cb) = 0;
};

// Foobar2000 IPC 实现
class FoobarIPCSource : public IDataSource { /* 命名管道通信实现 */ };
```

### 2. 歌词引擎层
```cpp
struct LyricLine {
    int64_t timeMs;
    std::wstring text;
    bool isTranslation; // 是否为翻译行
};

class LyricDocument {
public:
    std::vector<LyricLine> lines;
    int offsetMs;
    // 获取指定时间点的活跃歌词
    std::vector<LyricLine> GetActiveLines(int64_t currentTimeMs); 
};

class LyricParser {
public:
    // 处理文件编码并解析成 LyricDocument 对象
    static LyricDocument Parse(const std::wstring& filePath);
};

class LyricManager {
public:
    void LoadLyric(const std::wstring& audioFilePath);
    LyricDocument* GetCurrentLyric();
private:
    std::wstring FindLrcFile(const std::wstring& audioFilePath);
};
```

### 3. 渲染与窗口层
```cpp
struct RenderConfig {
    std::wstring fontFamily;
    float fontSize;
    Color color;
    // 渐变、描边、阴影等配置属性...
};

class OverlayRenderer {
public:
    OverlayRenderer(HWND hwnd);
    void RenderFrame(const std::vector<LyricLine>& activeLines, const RenderConfig& config);
private:
    // D2D / DWrite 资源
    ID2D1Factory* d2dFactory;
    ID2D1RenderTarget* renderTarget;
    IDWriteFactory* dwriteFactory;
    
    void DrawTextWithEffects(...);
};

class LayeredWindow {
public:
    void Create(int x, int y, int width, int height);
    void SetTransparent(bool isMousePassthrough);
    void UpdatePixels(const void* bgraPixels, int width, int height); // 封装 UpdateLayeredWindow
    HWND GetHWND();
};
```

---

## 五、 数据流向图 (Data Flow)

数据流主要分为三个核心场景：**播放进度推进 (Tick)**、**切歌/跳转 (Event)**、**配置变更 (Config)**。

### 1. 播放进度推进数据流 (Tick Sync Flow)
当 Foobar2000 正在正常播放时，系统依靠定时器刷新的数据流向：

```text
[定时器 Tick (例如 30Hz)]
       │
       ▼
[Render Thread] ─(获取)─> 共享内存/原子变量中的最新时间 (Current Time)
       │
       ▼
[Synchronizer] 根据当前时间，在 `LyricDocument` 中二分查找，得出当前应该显示的 `vector<LyricLine>`
       │
       ▼
[OverlayRenderer] 根据 JSON 配置的字体、描边、阴影，利用 DWrite/D2D 绘制画面到内存位图
       │
       ▼
[LayeredWindow] 调用 `UpdateLayeredWindow`，将带 Alpha 通道的位图提交给 DWM 窗口管理器
       │
       ▼
[Bilibili直播姬] 窗口捕获 (获取到纯净透明的歌词层)
```

### 2. 切歌 / 状态变更数据流 (Track Change Flow)
由播放器主动推送的事件流，确保“零延迟”响应用户的跳歌/切歌操作：

```text
[Foobar2000] 用户双击新歌 / 拖拽进度条
       │
       ▼
[foobar_plugin.dll] 通过 Named Pipe 发送 `TrackChanged` / `Seeked` 事件与最新文件路径
       │
       ▼
[IPC Receiver Thread] 接收到消息
       │
       ├─(如果是 Seek) ──> 更新共享时间变量 ──> 唤醒 Render Thread 立即重绘
       │
       └─(如果是 Track Changed)
               │
               ▼
        [LyricManager] 卸载旧歌词，根据新 Audio File Path 查找新 `.lrc`
               │
               ▼
        [LyricParser] 读取文件 -> 探测编码 -> 解析时间轴 -> 生成新 `LyricDocument`
               │
               ▼
        [File Watcher] 重新绑定目录监听
               │
               ▼
        [Render Thread] 触发重绘，显示新歌词
```

### 3. 热重载与配置数据流 (Hot-Reload Flow)
```text
[外部文本编辑器] 修改并保存 .lrc 文件 / [UI界面] 用户修改了阴影参数
       │
       ▼
[File Watcher / Config Manager] 检测到文件变更事件
       │
       ▼
向 [Main UI Thread] 发送更新消息
       │
       ▼
重新加载 `LyricDocument` 或重新读取 `RenderConfig`
       │
       ▼
清空对应的 D2D/DWrite 缓存，立即唤醒 [Render Thread] 刷新当前画面
```
