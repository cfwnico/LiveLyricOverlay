# LiveLyricOverlay 技术选型文档

## 大前提：Vibe Coding 友好
**Vibe Coding** 的核心理念是：让 AI 承担大量的代码编写工作，开发者主要负责架构设计、需求把控和“验收”。因此，所选技术栈必须具备以下特征，才能让 AI 发挥最大效能：
1. **强类型与清晰的语义**：让 AI 极难犯低级语法和类型错误。
2. **海量的训练数据**：生态成熟，API 稳定，AI “闭着眼睛都能写出最优解”。
3. **低心智负担的底层调用**：调用系统底层 API 时，有标准化的封装，避免 AI 在内存管理或指针上绕弯路。

基于以上原则，结合《总体架构设计文档》中极低资源占用（CPU<1%, RAM<50MB）、高刷新率（120Hz）和 Layered Window 的硬性要求，制定如下技术选型方案。

---

## 核心技术栈选择

### 一、 主程序 (Main Application)
* **语言与运行时**: **C# 12 / .NET 8**
* **应用形态**: 纯 Win32 API 封装 (通过 P/Invoke) 或极简的 WPF (配置为无边框透明)。建议使用 C# 原生控制 Win32 窗口生命周期。
* **Vibe Coding 契合度与优点**:
  * **极高的一次过率**: AI 对 C# 语法和 .NET 基础库的掌握达到了炉火纯青的地步。
  * **轻松驾驭底层 Win32**: AI 对 C# 的 P/Invoke（如 `UpdateLayeredWindow`, `SetWindowPos`, `RegisterHotKey` 等）了如指掌，几乎不会写出报错的签名。
  * **内存与性能**: .NET 8 支持 **Native AOT** 编译，可以彻底摆脱 JIT 开销，使内存占用极低（可轻松控制在 20MB 以内），启动时间达到毫秒级（满足 `<1秒` 的需求），完美解决传统 .NET 应用“臃肿”的痛点。

### 二、 图形与渲染引擎 (Graphics & Rendering)
* **框架选择**: **SkiaSharp** (Google Skia 的 .NET 绑定) 或 **Vortice.Windows** (Direct2D / DirectWrite 的精简绑定)。
* **首选推荐**: **SkiaSharp**。
* **Vibe Coding 契合度与优点**:
  * **绘图 API 极其语义化**: 相比原生 Direct2D 冗长的 COM 接口和工厂创建过程，SkiaSharp 的 API (如 `SKCanvas.DrawText`, `SKPaint`, `SKImageFilter.CreateDropShadow`) 非常符合人类直觉。AI 写起来如鱼得水，能在最短的代码行数内实现**描边、高斯模糊（发光/阴影）、渐变**等复杂效果。
  * **与 UpdateLayeredWindow 完美配合**: SkiaSharp 可以直接渲染到内存位图（BGRA），取出指针后直接丢给 Win32 API 即可完成带透明通道的桌面 Overlay。
  * **跨平台基因**: 虽然我们只做 Windows，但 Skia 稳定、高效，且文本排版功能非常完善。

### 三、 Foobar2000 数据源插件 (Foobar2000 Component)
* **语言与运行时**: **Modern C++ (C++17/20)**
* **框架选择**: 官方 **Foobar2000 SDK**
* **Vibe Coding 契合度与优点**:
  * 由于 Foobar2000 SDK 强制要求使用 C++，这里没有选择余地。
  * **化繁为简**: 我们将这个 C++ 插件的职责压缩到极简——**只读数据，不做任何计算**。AI 生成一段监听 `on_playback_time` 和 `on_playback_new_track` 的代码，并通过管道广播出去，成功率极高，几乎不需要反复调试。

### 四、 进程间通信 (IPC)
* **技术选择**: **命名管道 (Named Pipes)**
* **Vibe Coding 契合度与优点**:
  * Named Pipes 是 Windows 下本地进程间通信的最优解，延迟极低。
  * C++ (Win32) 端和 C# (`System.IO.Pipes`) 端都有着**极其标准的实现范式**。向 AI 下达“用 C++ 写一个 Named Pipe Server，用 C# 写一个 Client，收发 JSON 或二进制结构体”的指令，AI 可以一次性输出完全可用的双向通信代码。

### 五、 数据与配置解析 (Data Parsing)
* **LRC 歌词解析**: C# 原生正则 (`Regex`) 或字符串处理。AI 写 LRC 解析器（包含多时间轴、偏移量）可谓手到擒来，处理各种文本编码（通过 `System.Text.Encoding` 探测）也非常简便。
* **配置存储**: C# `System.Text.Json` (支持 AOT，性能极佳)，无需引入第三方库。

---

## 为什么不选其他技术栈？

1. **为什么不选 Electron / Tauri / WebView2 方案？**
   * **Vibe 友好但性能不符**：前端技术栈（HTML/CSS/JS）AI 写起来最快，也最容易做出漂亮的效果。但是，所有基于浏览器的方案都无法满足 `CPU < 1%` 和 `RAM < 50MB` 的硬核性能要求。而且 Chromium 在处理真正的全局透明、鼠标穿透时的性能损耗远大于原生绘制。
2. **为什么不全盘使用 C++ / Qt？**
   * **Vibe 阻力大**：C++ 遇到内存泄漏、依赖管理（CMake/vcpkg）、D2D 初始化繁琐等问题时，往往需要开发者投入大量精力去 Review AI 的代码，修复编译错误。心智负担较重，不适合以“快速验证、快速迭代”为主的 Vibe Coding 节奏。
3. **为什么不选 Rust？**
   * **GUI 生态割裂**：Rust 语法极度严谨，AI 生成的逻辑代码质量很高。但在 Windows 桌面 UI 与 Layered Window 的集成上，生态缺乏“一锤定音”的标准库。让 AI 去折腾 `windows-rs` 手动封装 `UpdateLayeredWindow`，加上处理借用检查器，可能会增加不必要的沟通成本。

## 结论

**C# 12 (Native AOT) + SkiaSharp + Named Pipe + C++ (极简 Foobar SDK 插件)** 的组合，是当前该项目兼顾**“极致性能目标”**与**“AI 辅助编程极爽体验 (Vibe Coding)”**的最佳技术栈黄金交叉点。
