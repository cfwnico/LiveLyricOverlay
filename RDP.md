---

# LiveLyricOverlay（暂定名）

## 软件定位

一个专门面向直播场景的 Windows 桌面歌词 Overlay。

设计目标：

> **为直播软件（尤其 Bilibili 直播姬）提供一个可捕获、真正 Alpha 透明、低延迟、高性能的歌词窗口。**

本软件**不负责下载歌词**。

本软件仅负责：

* 获取播放器状态
* 同步歌词
* 高质量渲染
* 输出透明窗口

播放器：

> **仅支持 foobar2000**

---

# 一、总体目标

实现：

```
foobar2000
      │
      │ 当前播放状态
      │ 当前时间
      │ 当前文件
      ▼
LiveLyricOverlay
      │
      ▼
透明歌词窗口
      │
      ▼
Bilibili直播姬
```

直播姬最终捕获：

```
歌词文字

描边

阴影

发光
```

除此之外：

Alpha=0

不能包含：

* 桌面
* Explorer
* Chrome
* Wallpaper
* ESLyric窗口
* 任何背景

---

# 二、设计原则

## 1）播放器唯一

仅支持：

```
foobar2000
```

不支持：

* Spotify
* QQ音乐
* 网易云
* MPC-HC
* VLC

后续可扩展。

---

## 2）歌词来源唯一

仅支持：

```
*.lrc
```

不读取：

ESLyric内部缓存。

原因：

* 与插件解耦
* 更稳定
* 更容易维护

---

## 3）同步来源唯一

不能靠：

```
计时器++
```

同步。

必须实时获取：

```
foobar当前播放位置
```

原因：

用户：

* 快进
* 后退
* 拖动进度条
* AB Repeat
* 跳歌

歌词必须立即同步。

---

# 三、歌词同步

必须支持：

普通LRC

例如：

```
[00:10.20]
Hello

[00:13.50]
World
```

也支持：

```
多个时间标签
```

例如：

```
[00:10.00][01:20.00]
Hello
```

必须支持。

---

# 四、时间同步

同步方式：

实时读取：

```
foobar播放位置
```

刷新率：

默认：

```
30Hz
```

可调：

```
15
30
60
120
```

刷新。

绝不能：

每秒一次。

---

## 歌词刷新

例如：

```
00:10.300
```

当前句。

```
00:10.310
```

仍然当前句。

```
00:13.500
```

下一句。

不允许：

累计误差。

---

# 五、歌词解析

支持：

UTF-8

UTF-8 BOM

ANSI

GBK

GB2312

UTF-16 LE

UTF-16 BE

自动检测。

---

支持：

```
[offset:+120]
```

支持：

```
[offset:-80]
```

支持：

空行。

支持：

注释。

支持：

翻译歌词。

例如：

```
[00:12]
Hello

[00:12]
你好
```

自动识别为：

主歌词

副歌词。

---

# 六、歌词显示

默认：

双行。

例如：

```
Hello

你好
```

也支持：

单行。

---

支持：

左对齐

右对齐

居中

---

支持：

自动换行。

---

支持：

最大宽度。

超过：

自动换。

---

支持：

单句最长：

不限。

---

# 七、文字效果

支持：

字体。

支持：

字号。

支持：

字重。

支持：

Italic。

支持：

字体颜色。

支持：

渐变。

支持：

透明度。

---

描边：

支持：

宽度：

0~20px

颜色

透明度

---

阴影：

支持：

偏移

模糊

颜色

透明度

---

发光：

支持：

颜色

半径

透明度

---

# 八、窗口

输出：

真正：

Layered Window

ARGB。

背景：

Alpha=0。

不是：

ColorKey。

不是：

截图桌面。

不是：

透明背景。

---

支持：

鼠标穿透。

支持：

置顶。

支持：

锁定位置。

支持：

自由拖动。

---

支持：

多显示器。

---

支持：

任意缩放。

---

# 九、直播兼容

重点：

兼容：

OBS

直播姬

抖音直播伴侣

快手直播伴侣

XSplit

窗口捕获。

---

# 十、性能

CPU：

目标：

<1%

静止歌词：

≈0%。

GPU：

几乎不占。

内存：

<50MB

启动：

<1秒

---

# 十一、配置

保存：

JSON。

支持：

多个配置。

例如：

```
直播

录播

游戏

音乐
```

---

# 十二、自动更新

v1：

不做。

---

# 十三、日志

Debug模式：

输出：

```
同步时间

当前歌词

刷新耗时

FPS
```

普通用户：

关闭。

---

# 十四、异常处理

没有歌词：

显示：

```
♪ No Lyrics
```

可关闭。

播放器退出：

自动隐藏。

播放器暂停：

歌词暂停。

播放器停止：

清空歌词。

跳歌：

立即同步。

---

# 十五、UI

极简。

只有：

```
位置

字体

颜色

效果

同步

高级
```

---

# 十六、未来版本（v2）

预留：

卡拉OK。

逐字。

动画。

淡入。

淡出。

歌词滚动。

SVG字体。

ASS字幕。

导出OBS。

WebSocket。

HTTP API。

---

# 十七、接口设计（重点）

这是我认为**整个项目最重要、也是最容易被忽略的一点**。

**必须通过 foobar2000 的官方接口获取播放状态，而不是靠 OCR、窗口标题或轮询猜测。**

需要的数据只有四项：

```text
当前播放文件完整路径
当前播放时间（毫秒）
播放状态（播放 / 暂停 / 停止）
播放变更通知（切歌、跳转、停止）
```

其中最关键的是：

* **播放位置**：用于歌词同步；
* **Seek（拖动进度）通知**：用于立即刷新当前歌词，而不是等下一次轮询；
* **Track Changed（切歌）通知**：用于重新加载对应的 `.lrc` 文件。

如果采用 foobar2000 SDK 开发一个配套组件（Component），可以直接监听这些事件，延迟和同步精度都会优于任何外部轮询方式。

---

# 十八、我检查后发现还需要补充的几个需求（容易遗漏）

这是我刚刚重新审查文档时发现遗漏的内容，我建议一开始就纳入 v1：

### ① LRC 文件查找策略（必须定义）

需要明确查找顺序，例如：

1. 与音频同目录同名 `.lrc`
2. 指定歌词目录（可配置）
3. （未来扩展）其他来源

否则不同用户的歌词管理方式会导致行为不一致。

### ② 文件监视

如果用户在播放过程中修改了 `.lrc` 文件（例如 ESLyric 下载完成并保存了歌词），程序应自动重新加载，而无需切歌。

### ③ 高 DPI 支持

窗口、字体、拖拽、阴影等都应正确适配 Windows 125%、150%、200% 缩放。

### ④ 渲染与数据彻底解耦

建议采用三层架构：

```text
Foobar 数据层
        ↓
歌词解析层
        ↓
渲染层
```

这样以后即使更换播放器或增加新的歌词来源，也无需修改渲染逻辑。

---